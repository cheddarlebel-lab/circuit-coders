node_modules/
.next/
.vercel
tsconfig.tsbuildinfo
.env*.local
.env.production
.env
.DS_Store
.last_lead_id
outreach-log/

# Secrets — never commit (live Stripe keys, comp IDs, tokens)
.secrets/
*.key
*.secrets
