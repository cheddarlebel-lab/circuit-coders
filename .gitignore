node_modules/
.next/
.vercel
tsconfig.tsbuildinfo
.env*.local
.env.production
.env
.DS_Store
.last_lead_id
outreach-log/

# Secrets — never commit (live Stripe keys, comp IDs, tokens)
.secrets/
*.key
*.secrets
clients/*/.secure/
clients/tsm/review-engine/.env

# Confidential client files — never commit to the (public until private) repo
clients/
SECURITY-INCIDENT-*.md

# Editor/backup scratch in the docs folder
public/docs/*.bak
